# =================================================================
# SkillGaug Development Environment Configuration
# =================================================================
# Copy this file to .env.local and update the values as needed
# Never commit .env.local to version control

# =================================================================
# Application Environment
# =================================================================
NODE_ENV=development

# =================================================================
# Database Configuration (PostgreSQL)
# =================================================================
POSTGRES_DB=skillgaug
POSTGRES_USER=skillgaug
POSTGRES_PASSWORD=skillgaug_dev_change_this
POSTGRES_PORT=5432

# Database URL for Prisma (constructed from above values)
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}

# =================================================================
# Redis Configuration
# =================================================================
REDIS_PASSWORD=skillgaug_redis_change_this
REDIS_PORT=6379
REDIS_URL=redis://:${REDIS_PASSWORD}@localhost:${REDIS_PORT}

# =================================================================
# JWT & Authentication
# =================================================================
# IMPORTANT: Generate strong secrets for production
# Use: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# NextAuth.js Configuration
NEXTAUTH_SECRET=your-super-secret-nextauth-key-change-this-in-production
NEXTAUTH_URL=http://localhost:3000

# =================================================================
# API Configuration
# =================================================================
API_PORT=4000
API_BASE_URL=http://localhost:4000

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# =================================================================
# Web Frontend Configuration
# =================================================================
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_APP_NAME=SkillGaug
NEXT_PUBLIC_APP_VERSION=1.0.0

# =================================================================
# Code Execution & Scoring (Future Implementation)
# =================================================================
# Docker execution settings
SCORING_TIMEOUT_SEC=30
SCORING_MEMORY_LIMIT=512m
SCORING_CPU_LIMIT=1

# Docker images for different languages
SCORING_IMAGE_PYTHON=python:3.11-slim
SCORING_IMAGE_JAVASCRIPT=node:20-alpine
SCORING_IMAGE_JAVA=openjdk:21-slim

# =================================================================
# Email Configuration (Future Implementation)
# =================================================================
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=noreply@skillgaug.com

# =================================================================
# Logging & Monitoring
# =================================================================
LOG_LEVEL=debug
LOG_FORMAT=dev

# =================================================================
# Development Tools (MCP - Development Only)
# =================================================================
# MCP Integration (Local Development Only)
E2E_USE_MCP=true
MCP_PLAYWRIGHT_ENDPOINT=http://localhost:8080
MCP_SERENA_ENDPOINT=http://localhost:8081

# =================================================================
# Security Settings
# =================================================================
# Rate limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session settings
SESSION_TIMEOUT_MINUTES=480

# =================================================================
# Feature Flags (Future Implementation)
# =================================================================
# FEATURE_AI_PROBLEM_GENERATION=false
# FEATURE_SKILL_MAP_INTEGRATION=false
# FEATURE_ADVANCED_ANALYTICS=false